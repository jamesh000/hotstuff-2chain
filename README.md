# hotstuff 2chain
2chain hotstuff implementation in Golang, largely converted from asonnino/hotstuff. Consensus itself is fully working, with only some polish needed. The mempool/payload section of the program is still in debugging stage.

AI was used to convert consensus/timer.go, but everything else is by hand.
